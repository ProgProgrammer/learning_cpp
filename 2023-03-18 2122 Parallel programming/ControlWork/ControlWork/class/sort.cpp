#include "sort.h"

std::vector<std::vector<int>> Sort::sort(std::vector<int> arr)
{
    std::vector<std::vector<int>> arr_result;

    return arr_result;
}

std::vector<int> Sort::arrUnion(std::vector<std::vector<int>> arr)
{
    std::vector<int> arr_result;
    return arr_result;
}

std::vector<int> Sort::startLoop(std::vector<int> arr)
{
    sort(arr);

    return arr;
}